# บันทึกการรีวิวโค้ด d_homework.js

**คำแนะนำที่เลือกมาแก้ในโค้ดจริง:**
เลือกแก้โดยใช้ Object Destructuring ในฟังก์ชัน Callback (เช่น เปลี่ยน `member => member.name` เป็น `({ name, role }) => ...`)
*เหตุผล:* ทำให้โค้ดสั้นลง อ่านง่ายขึ้นมาก และเห็นได้ชัดเจนทันทีว่าในฟังก์ชันนั้นเราต้องการดึงค่าอะไรจาก Object มาใช้งานบ้าง

**คำแนะนำที่ไม่เอามาแก้:**
ไม่เอาการสร้างตัวแปรคงที่ (Constants) แยกต่างหากเพื่อเก็บค่า "PO", "Dev", "SM"
*เหตุผล:* โค้ดต้นฉบับยังมีขนาดเล็กและใช้งาน array แค่ตัวเดียว การใช้ตัวอักษร String โดยตรงยังอ่านและดูแลได้ง่าย การทำ Constants ในตอนนี้ยังไม่จำเป็นและอาจทำให้โค้ดดูซับซ้อนเกินไป