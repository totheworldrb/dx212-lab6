# บันทึกการทดลอง Prompt Engineering (Lab Sheet 6)

## Part A1 (Prompt แบบแย่)
**Prompt:** เขียนฟังก์ชันคำนวณค่ารถให้หน่อย
**ผลลัพธ์ที่ได้:
function calculateFare(distance, ratePerKm) {
    let totalFare = distance * ratePerKm;
    return totalFare;
}
// ตัวอย่างการใช้งาน: ระยะทาง 15 กิโลเมตร, กิโลเมตรละ 5 บาท
let myFare = calculateFare(15, 5);
console.log("ค่ารถทั้งหมด: " + myFare + " บาท");

## Part A2 calcFare
**Prompt:** 
คุณคือนักพัฒนา JavaScript อาวุโส
เขียนฟังก์ชัน calcFare(distanceKm)
สำหรับแอปรถ NGV ในมหาวิทยาลัย: 2 กม.แรก 10 บาท กม.ถัดไปคิด กม.ละ 2 บาท เศษของกิโลเมตรปัดขึ้น ระยะทางติดลบหรือไม่ใช่ตัวเลขให้คืน 0
ใช้ ES6 arrow function, ไม่ใช้ library ภายนอก, มี comment ภาษาไทยสั้น ๆ
ตอบเป็นโค้ดอย่างเดียว ตามด้วย console.log ทดสอบ 3 กรณี: 1.5, 2, 7.2

**ผลลัพธ์ย่อ:** ได้ฟังก์ชัน arrow function ที่ใช้ Math.ceil() ในการปัดเศษขึ้น และมี console.log 3 กรณีตามที่สั่ง
**ตรวจแล้ว:** ใช้ได้ (รันแล้วได้ 10, 10, 22 ตรงตามที่คำนวณมือ)
**รอบที่ iterate:** 1 รอบ (ผ่านตั้งแต่ครั้งแรก)

---

## Part B (การอธิบายโค้ด)
**ผลการตรวจสอบคำอธิบายจาก AI:** ตรวจแล้วถูก (AI สามารถอธิบายการทำงานของ map/filter/reduce ได้อย่างถูกต้องและไม่มีเนื้อหาที่เกินจริง)

---

## Part D User Story (ฟังก์ชันกรองเมนูตามงบ)
**Prompt:**
คุณคือนักพัฒนา JavaScript เขียนฟังก์ชันสำหรับการกรองรายการเมนูอาหารตามงบประมาณ (User story: ในฐานะผู้ใช้ ฉันอยากเห็นรายการเมนูที่ราคาไม่เกินงบ)
เงื่อนไข:
1. ขอเป็นฟังก์ชัน JavaScript ล้วน ที่ทำงานกับ Array of object
2. สร้างตัวอย่างข้อมูลจำลอง (Mock Data) มา 5 เมนู
3. มี console.log ทดสอบ 3 กรณี รวมกรณีขอบเขต 1 กรณี

**ผลลัพธ์ย่อ:** ได้ฟังก์ชันชื่อ filterMenusByBudget มีการใช้ .filter() พร้อม Mock Data รายการอาหาร 5 อย่าง และกรณีทดสอบครบ 3 กรณี
**ตรวจแล้ว:** ใช้ได้ (ทำงานได้ถูกต้องและดักจับกรณีขอบเขตได้)
**รอบที่ iterate:** 1 รอบ