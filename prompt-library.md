## 1. Prompt สำหรับ "อธิบายโค้ด" (Explain Code)
**Prompt เต็ม:**
> อธิบายโค้ดต่อไปนี้ทีละบรรทัดให้นักศึกษาปี 2 ที่เพิ่งเรียน [ระบุหัวข้อ เช่น JavaScript Array Methods] เข้าใจ ตอบเป็นภาษาไทย 
> รูปแบบ: ตาราง 2 คอลัมน์ (บรรทัดโค้ด | ทําอะไร) ไม่เกิน 10 แถว
> 
> [แปะโค้ดที่นี่]

**ใช้กับสถานการณ์ไหน:** 
เมื่อไปเจอโค้ดของคนอื่น หรือตัวอย่างโค้ดในเน็ตที่ไม่เข้าใจ และต้องการให้ย่อยข้อมูลให้เข้าใจง่ายๆ แบบเห็นภาพรวมทีละบรรทัด

**ข้อควรระวัง 1 ข้อ:** 
AI อาจมีอาการ "Hallucination" (คิดไปเอง) โดยอาจอธิบายการทำงานของฟังก์ชันภายนอกผิด หรือเดาบริบทของโค้ดผิด ควรตรวจสอบกับ Document อ้างอิงอีกครั้ง

## 2. Prompt สำหรับ "สร้างฟังก์ชันจาก User Story" (Generate Code)
**Prompt เต็ม:**
> คุณคือนักพัฒนา Senior JavaScript 
> เขียนฟังก์ชัน [ระบุชื่อฟังก์ชัน] จาก User Story: "[ระบุ User Story เช่น ในฐานะผู้ใช้ ฉันอยากเห็นรายการเมนูที่ราคาไม่เกินงบ]" 
> โดยมีข้อจำกัดคือ: เป็นฟังก์ชัน JavaScript ล้วน (ES6 Arrow Function), ทำงานกับ Array of Object และไม่ใช้ UI 
> รูปแบบการตอบ: ตอบเป็นโค้ดอย่างเดียว พร้อมสร้างตัวอย่างข้อมูลจำลอง (Mock Data) 5 รายการ และมี console.log ทดสอบ 3 กรณี (โดยให้มีกรณีขอบเขต/Edge case 1 กรณี)

**ใช้กับสถานการณ์ไหน:** 
เมื่อได้รับ Requirement มาจากทีม (เช่น ในบอร์ด Trello) และต้องการขึ้นโครงสร้างโค้ดเริ่มต้นที่รันและทดสอบได้ทันที

**ข้อควรระวัง 1 ข้อ:** 
User Story มักจะบอกข้อมูลไม่ครบ ก่อนใช้ Prompt นี้สั่งเขียนโค้ด ควรใช้เทคนิค "ให้ AI ถามคำถาม 3 ข้อเพื่อเคลียร์ Requirement" ก่อนเสมอ เพื่อลดการแก้ไขโค้ดหลายรอบ

## 3. Prompt สำหรับ "ดีบักหาข้อผิดพลาด" (Debug)
**Prompt เต็ม:**
> โค้ดนี้มีบั๊ก
> - ผลลัพธ์/Error ที่ได้จริง: [ระบุ Error หรือผลที่ได้ เช่น คืนค่าเป็น Array ว่าง]
> - สิ่งที่คาดหวัง: [ระบุผลลัพธ์ที่ถูกต้อง]
> - สิ่งที่ฉันเดาว่าผิด: [ระบุจุดที่สงสัย เช่น อาจจะลืมใส่ return หรือกำหนดค่าเริ่มต้นผิด]
> 
> คำสั่ง: ช่วยอธิบายสาเหตุของบั๊กนี้ก่อน แล้วค่อยให้โค้ดที่แก้ไขแล้ว โดยอย่าเปลี่ยนโครงสร้างส่วนที่ไม่เกี่ยวข้อง
>
> [แปะโค้ดที่นี่]

**ใช้กับสถานการณ์ไหน:** 
เมื่อรันโปรแกรมแล้วเกิด Error หรือได้ผลลัพธ์ไม่ตรงตามที่คิดไว้ และต้องการหาจุดบกพร่องโดยไม่ให้ AI เขียนโค้ดใหม่ทั้งหมดทับโค้ดเดิม

**ข้อควรระวัง 1 ข้อ:** 
อย่าโยนแค่โค้ดให้ AI ดูเฉยๆ ต้องระบุ Error Message หรือผลลัพธ์ที่ผิดพลาดแนบไปด้วยเสมอ ไม่อย่างนั้น AI อาจจะไปแก้จุดอื่นที่ไม่ได้มีปัญหาแทน

## 4. Prompt สำหรับ "รีวิวและปรับปรุงโค้ด" (Code Review)
**Prompt เต็ม:**
> รีวิวโค้ดนี้ในฐานะ Senior Developer 
> ช่วยบอกจุดที่ควรปรับปรุง 3 ข้อ เรียงตามความสําคัญ (เช่น เรื่อง Clean Code, ประสิทธิภาพ, หรือการจัดการ Error) 
> พร้อมอธิบายเหตุผลสั้นๆ และแนบโค้ดเวอร์ชันที่ปรับปรุงแล้วมาให้ดูเป็นตัวอย่าง
>
> [แปะโค้ดที่นี่]

**ใช้กับสถานการณ์ไหน:** 
เมื่อเขียนโค้ดเสร็จแล้ว รันผ่านแล้ว แต่อยากยกระดับคุณภาพโค้ดให้เป็นมืออาชีพมากขึ้น (Refactoring) อ่านง่ายขึ้น หรือทำงานเร็วขึ้น

**ข้อควรระวัง 1 ข้อ:** 
AI มักจะชอบแนะนำสิ่งที่ "Over-engineering" (ซับซ้อนเกินความจำเป็น) เราควรเลือกนำมาปรับใช้เฉพาะข้อที่เราเข้าใจและเหมาะสมกับขนาดของโปรเจกต์จริงๆ เท่านั้น

## 5. Prompt สำหรับ "เขียน Test Case" (Testing)
**Prompt เต็ม:**
> ช่วยออกแบบ Test Case สำหรับฟังก์ชัน [ระบุชื่อฟังก์ชัน] ที่ทำหน้าที่ [อธิบายหน้าที่ของฟังก์ชันสั้นๆ] 
> ขอให้ครอบคลุมทั้งกรณีปกติ (Happy Path), กรณีที่ข้อมูลผิดพลาด (Error Handling), และกรณีขอบเขต (Edge Cases) 
> ตอบในรูปแบบตารางที่มี 4 คอลัมน์: กรณีทดสอบ | ข้อมูลที่ใส่ (Input) | ผลลัพธ์ที่คาดหวัง (Expected Output) | เหตุผล

**ใช้กับสถานการณ์ไหน:** 
ใช้ตอนที่ต้องการวางแผนการทดสอบระบบล่วงหน้า หรือต้องการเช็คลิสต์เพื่อดูว่าฟังก์ชันที่เราเขียนขึ้นมารองรับความเป็นไปได้ครบทุกรูปแบบหรือยัง

**ข้อควรระวัง 1 ข้อ:** 
AI อาจลืมนึกถึงกรณีที่ผู้ใช้กรอกข้อมูลแปลกๆ (เช่น ค่า Null, Undefined, String ว่าง) ควรพิจารณาเพิ่ม Test case เกี่ยวกับ Data type ผิดประเภทด้วยตัวเองเสมอ